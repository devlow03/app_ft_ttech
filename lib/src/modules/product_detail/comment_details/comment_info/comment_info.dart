import 'package:flutter/material.dart';
    
class CommentInfo extends StatelessWidget {

  const CommentInfo({ Key? key }) : super(key: key);
  
  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        
      ],
    );
  }
}