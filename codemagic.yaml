workflows:
  ios:
    name: iOS Build
    environment:
      flutter: stable
      xcode: latest
    scripts:
      - name: Clean CocoaPods
        script: |
          cd ios
          pod clean
      - name: Install CocoaPods
        script: |
          cd ios
          pod install
      - flutter build ios --release --no-codesign
